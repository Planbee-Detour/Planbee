.DS_Store
.idea/
.vscode/
*.iml
.env
.env.*
!.env.example

# 에이전트 CLI 로컬 생성물 — `make harness` 가 다시 만든다.
# git 에 올라가는 원본은 AGENTS.md 와 .agents/ 뿐이다.
/CLAUDE.md
/GEMINI.md
/.claude/
/.gemini/
/.codex/

mobile/node_modules/
mobile/ios/Pods/
mobile/ios/build/
mobile/android/.gradle/
mobile/android/app/build/
server/.gradle/
server/build/

# 약관 본문은 docs/legal/*.md 에서 생성한다 (`make legal-bundle`).
# 원본이 저장소 안에 있으므로 사본까지 커밋하면 둘이 갈라질 수 있다 — 사본만 제외한다.
# (계약에서 생성하는 mobile/src/shared/api/schema.ts 는 예외적으로 커밋한다 —
#  모바일만 clone 해도 타입이 맞아야 하고, 계약 변경이 diff 로 드러나야 하기 때문이다.)
mobile/src/features/auth/legal/documents.generated.ts
