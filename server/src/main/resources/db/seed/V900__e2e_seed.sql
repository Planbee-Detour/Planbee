-- E2E 시드 데이터. e2e 프로필에서만 적용된다. (application-e2e.properties)
--
-- 규칙
-- 1. 결정론적이어야 한다. 랜덤 값이나 now() 를 쓰지 않는다 — 매 실행 같은 상태여야 테스트가 안정된다.
-- 2. 시드는 V900 이상 번호를 쓴다. 실제 스키마 마이그레이션(V1~)과 번호가 겹치면 안 된다.
-- 3. 로그인 계정을 추가할 때 비밀번호는 반드시 해시로 넣는다. (server.md S-17)
-- 4. 여기 있는 계정과 데이터는 e2e/flows/*.yaml 이 그대로 참조한다. 바꾸면 플로우도 함께 고친다.
--
-- TODO: auth / schedule 도메인이 생기면 테스트 계정과 기본 일정을 여기에 추가한다.

SELECT 1;
