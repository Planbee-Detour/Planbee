-- 베이스라인 마이그레이션.
-- 스키마의 소유자는 Flyway 다. 엔티티를 추가할 때 반드시 여기에 마이그레이션을 함께 넣는다.
-- Hibernate 는 ddl-auto=validate 로 검증만 하므로, 마이그레이션이 없으면 애플리케이션이 뜨지 않는다.
--
-- 명명 규칙: V<번호>__<설명>.sql  (예: V2__create_schedule.sql)
-- 이미 적용된 마이그레이션 파일은 수정하지 않는다. 항상 새 버전을 추가한다.

SELECT 1;
